namespace TestTask1
{
	// Due to the existence of the "none" type, 
	// it is impossible to use a generalized class,
	// so it is easier to store values in a string,
	// and store their type using the enumeration
	public enum TagType
	{
		Bool,
		Int,
		Double,
		None
	}
}
